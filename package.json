{
  "name": "media-engine",
  "version": "0.0.0",
  "private": true,
  "description": "Open source TypeScript engine for unified media metadata search and aggregation.",
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
