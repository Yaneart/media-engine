// TorrServer file IDs cross the runtime/session boundary and must share one bound.
// ID файлов TorrServer пересекают runtime/session boundary и используют одну границу.
export const MAX_ORIGINAL_TORRENT_FILE_ID = 1_000_000;
